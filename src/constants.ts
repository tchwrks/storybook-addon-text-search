export const ADDON_ID = "textsearch-addon";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = ADDON_ID;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};

// Responsive breakpoints for SearchBar
export const __MOBILE_BREAKPOINT_: number = 500;
